# Design references

The local `sources/` directory contains private working screenshots used to
compare the extension with ChatGPT and Codex interface patterns. It is excluded
from Git because those screenshots may contain conversation text.

The resulting implementation principles are recorded in
[`../reuse-matrix.md`](../reuse-matrix.md) and
[`../product-scope.md`](../product-scope.md) without reproducing private content.
